% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters.R
\name{wavelet_heat_knn}
\alias{wavelet_heat_knn}
\title{Heat wavelet transform on a k-NN graph (convenience)}
\usage{
wavelet_heat_knn(
  coords,
  signal,
  scales,
  k = 6,
  weight = c("heat", "binary", "distance"),
  sigma = NULL,
  sym = c("union", "mutual"),
  normalized = FALSE,
  seed = NULL,
  K = 30,
  lmax = NULL,
  jackson = FALSE
)
}
\arguments{
\item{coords}{numeric matrix of node coordinates (n x d)}

\item{signal}{vector or matrix of signals (n x m)}

\item{scales}{numeric vector of heat scales}

\item{k}{neighbors per node (passed to \code{\link[=graph_knn]{graph_knn()}})}

\item{weight}{weighting scheme for k-NN edges (\code{"heat"}, \code{"binary"}, \code{"distance"})}

\item{sigma}{optional bandwidth for \code{weight = "heat"}; defaults to median k-NN distance}

\item{sym}{symmetrization mode for k-NN graph (\code{"union"} or \code{"mutual"})}

\item{normalized}{logical; compute normalized Laplacian}

\item{seed}{optional RNG seed}

\item{K}{Chebyshev order}

\item{lmax}{optional spectral radius}

\item{jackson}{logical; apply Jackson damping}
}
\value{
list of filtered signals, one per scale (as in \code{\link[=wavelet_heat_transform]{wavelet_heat_transform()}})
}
\description{
Build a k-nearest-neighbor graph from coordinates and apply the heat wavelet
transform in one step.
}
