% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reduction.R
\name{tree_multiresolution}
\alias{tree_multiresolution}
\title{Tree multiresolution by even-depth downsampling}
\usage{
tree_multiresolution(
  g,
  levels,
  root = 1L,
  reduction_method = c("resistance_distance", "sum", "unweighted")
)
}
\arguments{
\item{g}{A \code{gsp_graph} representing an undirected, connected tree.}

\item{levels}{Number of coarsening levels.}

\item{root}{Root vertex index for defining depths (1-based).}

\item{reduction_method}{How to combine weights along length-2 paths:
\code{"resistance_distance"} uses series resistance (\code{1/(1/w1 + 1/w2)}),
\code{"sum"} uses \code{w1 + w2}, and \code{"unweighted"} sets all weights to 1.}
}
\value{
A list with:
\describe{
\item{graphs}{list of graphs at each level (level 0..levels)}
\item{keeps}{list of kept indices at each level (in previous graph)}
\item{roots}{root index at each level (in that level's graph)}
}
}
\description{
Builds a multiresolution sequence from a tree by keeping even-depth vertices
(with respect to a chosen root) and coarsening by connecting each kept node
to its grandparent in the original tree. Edge weights are combined using a
simple rule.
}
