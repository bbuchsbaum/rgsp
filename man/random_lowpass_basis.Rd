% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress.R
\name{random_lowpass_basis}
\alias{random_lowpass_basis}
\title{Randomized low-pass basis via Chebyshev filtering (no eigenpairs)}
\usage{
random_lowpass_basis(
  g,
  k,
  kernel = c("heat", "step"),
  tau = NULL,
  cutoff = NULL,
  K = 30,
  oversample = 16,
  jackson = TRUE,
  lmax = NULL,
  block = NULL,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{g}{gsp_graph object}

\item{k}{target basis size}

\item{kernel}{low-pass kernel; one of "heat", "step", or a function
accepting lambda. For "heat", provide \code{tau}; for "step", provide \code{cutoff}.}

\item{tau}{diffusion time for heat kernel (ignored unless kernel = "heat").
If NULL, chosen so that lambda_c = 0.05 * lmax (i.e., tau = 1 / lambda_c).}

\item{cutoff}{step cutoff (keeps lambda <= cutoff); if NULL and kernel =
"step", defaults to 0.05 * lmax.}

\item{K}{Chebyshev order. If missing, defaults to 30 (or 50 when
\code{jackson = FALSE}).}

\item{oversample}{oversampling dimension; if missing, defaults to
\code{max(16, ceiling(0.1 * k))}.}

\item{jackson}{logical; apply Jackson damping to Chebyshev coefficients}

\item{lmax}{optional spectral radius (else computed)}

\item{block}{optional number of probe columns to filter at once (for memory).
If NULL, chosen to target ~300 MB per filtered probe block (keeps memory in
the 200-500 MB range), capped at the total number of probes.}

\item{seed}{optional RNG seed for reproducibility}

\item{verbose}{logical; print progress messages (default FALSE)}
}
\value{
list with elements:
\describe{
\item{loadings}{n x k orthonormal basis (columns)}
\item{lambdas}{length-k Ritz eigenvalue estimates}
\item{lmax}{spectral radius used}
\item{kernel}{kernel identifier}
\item{K}{Chebyshev order}
\item{block}{filtered probe block size used}
}
}
\description{
Builds a k-dimensional, graph-smooth orthonormal basis by filtering random
probes with a low-pass kernel approximated by Chebyshev polynomials. No
eigen-decomposition of the Laplacian is required.
}
