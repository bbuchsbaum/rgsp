% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparsify.R
\name{graph_sparsify_spectral}
\alias{graph_sparsify_spectral}
\title{Spectral graph sparsification (Spielman–Srivastava edge sampling)}
\usage{
graph_sparsify_spectral(
  g,
  epsilon,
  maxiter = 10,
  seed = NULL,
  n_probes = NULL,
  reg = NULL,
  q = NULL
)
}
\arguments{
\item{g}{A \code{gsp_graph} (undirected).}

\item{epsilon}{Sparsification parameter in \verb{[1/sqrt(n), 1)}. Smaller values
sample more edges (closer to original).}

\item{maxiter}{Maximum number of attempts. If a sparsified graph is
disconnected, \code{epsilon} is reduced toward \code{1/sqrt(n)} and retried.}

\item{seed}{Optional RNG seed for reproducibility.}

\item{n_probes}{Number of random probe vectors used to approximate effective
resistances (default scales with \code{log(n)} and is capped).}

\item{reg}{Diagonal regularization added to the combinatorial Laplacian for
linear solves (default scaled to graph degree).}

\item{q}{Number of edge samples (with replacement). If \code{NULL}, uses a
Spielman–Srivastava-inspired heuristic \code{q ~ 0.16 * n * log(n) / epsilon^2}.}
}
\value{
A new \code{gsp_graph} with a sparser adjacency.
}
\description{
Sparsifies an undirected graph by sampling edges proportionally to
approximate effective resistance, yielding a reweighted sparse graph whose
Laplacian approximately preserves the quadratic form.
}
\details{
This is a practical, R-native approximation intended to reduce \code{nnz} and
speed up downstream routines (Chebyshev filtering, TV, diffusion, etc.).
}
