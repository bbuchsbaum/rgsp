% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphs.R
\name{graph_knn}
\alias{graph_knn}
\alias{knn_graph}
\title{k-nearest-neighbor graph}
\usage{
graph_knn(
  coords,
  k = 6,
  weight = c("heat", "binary", "distance"),
  sigma = NULL,
  sym = c("union", "mutual"),
  normalized = FALSE,
  seed = NULL,
  eps = 1e-08
)

knn_graph(
  coords,
  k = 6,
  weight = c("heat", "binary", "distance"),
  sigma = NULL,
  sym = c("union", "mutual"),
  normalized = FALSE,
  seed = NULL,
  eps = 1e-08
)
}
\arguments{
\item{coords}{numeric matrix of node coordinates (n x d)}

\item{k}{number of neighbors per node (clipped to \code{n-1})}

\item{weight}{weighting scheme: \code{"heat"}, \code{"binary"}, or \code{"distance"}}

\item{sigma}{optional bandwidth for \code{weight = "heat"}; defaults to the median
k-NN distance}

\item{sym}{symmetrization mode: \code{"union"} (default) or \code{"mutual"}}

\item{normalized}{logical; if TRUE compute normalized Laplacian}

\item{seed}{optional RNG seed for reproducibility}

\item{eps}{small constant to avoid divide-by-zero for distance weights}
}
\value{
\code{gsp_graph} object
}
\description{
Build an undirected k-NN graph from node coordinates. Supports simple binary
weights, inverse-distance weights, or a heat kernel with an optional
bandwidth parameter. Uses \code{FNN::get.knn()} when available and falls back to a
distance matrix for very small graphs.
}
