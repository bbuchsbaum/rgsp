% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wavelet_transform.R
\name{wavelet_stat_map}
\alias{wavelet_stat_map}
\title{Wavelet-space statistic mapped back to nodes}
\usage{
wavelet_stat_map(
  g,
  X,
  stat = mean,
  bands = NULL,
  wavelet = "mexican_hat",
  include_lowpass = TRUE,
  K = 30,
  lmax = NULL,
  normalize = FALSE,
  ...
)
}
\arguments{
\item{g}{gsp_graph object}

\item{X}{numeric vector or matrix (n_nodes x n_signals)}

\item{stat}{function applied across signals for each coefficient
(default: \link{mean})}

\item{bands}{optional bands to use in the reconstruction (passed to \code{\link[=isgwt]{isgwt()}})}

\item{wavelet}{wavelet type (passed to \code{\link[=sgwt]{sgwt()}})}

\item{include_lowpass}{logical; include scaling function (default: TRUE)}

\item{K}{Chebyshev order (default: 30)}

\item{lmax}{spectral radius (computed if NULL)}

\item{normalize}{logical; if TRUE, divide aggregated coeffs by their max
absolute value before reconstruction}

\item{...}{extra arguments passed to \code{stat}}
}
\value{
list with elements:
\describe{
\item{map}{node-level reconstruction of the aggregated statistic}
\item{coeffs}{aggregated coefficients (n_nodes x n_bands)}
\item{sgwt}{\code{sgwt_coeffs} object with aggregated coeffs}
}
}
\description{
Runs the SGWT, aggregates coefficients across signals with a statistic
(e.g., mean/median), and reconstructs the aggregated result to the node
domain. Useful for quickly visualizing group-level patterns.
}
\examples{
g <- graph_ring(20)
X <- matrix(rnorm(20 * 5), nrow = 20)  # 5 signals on 20 nodes
res <- wavelet_stat_map(g, X, stat = mean)
plot(res$map, type = "h")

}
