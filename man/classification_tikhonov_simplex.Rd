% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learning.R
\name{classification_tikhonov_simplex}
\alias{classification_tikhonov_simplex}
\title{Graph Tikhonov classification with simplex constraints}
\usage{
classification_tikhonov_simplex(
  g,
  y,
  mask = NULL,
  tau = 0.1,
  tol = 1e-06,
  maxit = 200,
  lmax = NULL,
  step = NULL
)
}
\arguments{
\item{g}{graph}

\item{y}{integer/factor labels of length \code{g$n}; missing labels may be \code{NA}}

\item{mask}{logical vector of observed nodes (length \code{g$n}); if \code{NULL},
defaults to \code{!is.na(y)}}

\item{tau}{regularization strength (must be > 0)}

\item{tol}{stopping tolerance on relative iterate change}

\item{maxit}{maximum iterations}

\item{lmax}{optional spectral radius for step size (defaults to \code{lambda_max(g)})}

\item{step}{optional step size; if \code{NULL}, uses \code{0.5/(1 + tau*lmax)}}
}
\value{
matrix of class logits (n x K) with rows summing to 1 and non-negative
}
\description{
Semi-supervised classification with missing labels (mask). Solves
}
\details{
\deqn{\arg\min_X \|M X - Y\|_F^2 + \tau \, \mathrm{tr}(X^\top L X) \quad
\text{s.t. each row of } X \text{ lies on the simplex}}

where \code{Y} is a one-hot encoding of the observed labels and \code{M} is a diagonal
masking operator (observed rows).

This is a port of \code{pygsp.learning.classification_tikhonov_simplex} using a
projected (proximal) gradient method.
}
